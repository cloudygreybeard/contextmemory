#!/bin/bash
# Test CLI script to simulate version 0.5.0 for compatibility testing

if [ "$1" = "--version" ]; then
    echo "cmctl version 0.5.0"
    exit 0
fi

if [ "$1" = "health" ]; then
    echo "Storage health: OK"
    exit 0
fi

# For other commands, delegate to real cmctl
exec cmctl "$@"
